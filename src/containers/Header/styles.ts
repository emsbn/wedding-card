import styled from '@emotion/styled';

const Header = styled.header`
  position: sticky;
  //position: fixed;
  max-width: 600px;
  width: 100%;
  top: 0;
  z-index: 999;
  background: red;
`;

export { Header };
