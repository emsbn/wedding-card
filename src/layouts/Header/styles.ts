import styled from '@emotion/styled';

const Header = styled.header`
  position: fixed;
  max-width: 600px;
  width: 100%;
  height: 3rem;
  top: 0;
  z-index: 999;
  background: red;
`;

export { Header };
