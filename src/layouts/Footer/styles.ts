import styled from '@emotion/styled';

const Footer = styled.div`
  max-width: 600px;
  width: 100%;
  display: flex;
  justify-content: center;
  position: fixed;
  bottom: 0;
  background: violet;
`;

export { Footer };
